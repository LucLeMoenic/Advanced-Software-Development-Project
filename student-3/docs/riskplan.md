# Local Experience & Attraction Recommender Risk Plan

## 1. Method

Review this register at the start and end of each feature-plan chunk and before any shared Compose, CI, model, schema, or public API change.

- Probability (P): 1 rare, 2 unlikely, 3 possible, 4 likely, 5 almost certain.
- Impact (I): 1 negligible, 2 minor, 3 moderate, 4 major, 5 release/marks threatening.
- Score: `P x I`. Treat 15–25 as critical, 8–12 as high, 4–6 as medium, and 1–3 as low.

## 2. Risk Register

| ID | Risk and consequence | P | I | Score | Prevention / mitigation | Trigger and contingency | Evidence / review point |
|---|---|---:|---:|---:|---|---|---|
| R-01 | Feature is only reachable standalone (`student-3/frontend` alone), not from the unified home page/integrated Compose app — the rubric gives zero marks for non-integrated features. | 2 | 5 | 10 | Confirm the shared home page links to `/api/attractions` via `student3-frontend`; always demo/screenshot from the integrated Compose stack, never the standalone dev server. | If the unified page cannot reach the feature, stop and re-check `nginx.conf`/Compose wiring before continuing other work. | Shared home-page screenshot + integrated Compose evidence. |
| R-02 | Qwen (`qwen2.5` full-size, no tag) gets OOM-killed by Ollama under Docker Desktop's default ~3.7GB memory allocation. **Already occurred and was resolved.** | 5 | 3 | 15→0 (resolved) | Pinned to `qwen2.5:3b` (matching Student 1's `llama3.2:3b` sidestep of the same limit); documented in `prompt-log.md`. | If the model container is OOM-killed again (e.g. after a model-tag change), check `OLLAMA_MODELS`/`STUDENT3_MODEL` before assuming a code bug. | `prompt-log.md` "Operational note: model sizing." |
| R-03 | The LLM invents attractions that don't exist in the seeded catalogue, which would look broken live in the showcase. | 3 | 4 | 12 | Closed-context prompting: only attractions the database query actually returned (max 6) are supplied, with an explicit "recommend only from this list" instruction; `_is_response_usable()` rejects a response that names none of the supplied candidates. | If a demo response still mentions an invented attraction, that's an `_is_response_usable()` gap — tighten the on-topic check before the showcase, don't just re-roll the prompt. | `test_recommend.py`; live Ollama verification noted in `prompt-log.md`. |
| R-04 | CRUD exists in the backend/database API but not in the frontend UI, so the feature fails the "CRUD through the frontend" requirement even though the API is complete. **Identified as a genuine Release 0 gap; resolved in PR #38.** | 4 | 4 | 16→0 (resolved, pending merge/verification) | Added create/edit/delete UI for attractions and a review-submission form (PR #38), following the existing HTMX/`escapeHtml` conventions. | If PR #38 is not merged before the report/video, the feature reverts to backend-only CRUD and the risk re-opens at full severity. | PR #38 diff review; browser click-through checklist in `known-issues.md`. |
| R-05 | HTMX forms submit as `x-www-form-urlencoded` by default, but `create_attraction`/`update_attraction`/`create_review` only accept JSON (`request.get_json(silent=True)`, no form fallback) — new forms would silently 400. **Discovered and resolved during PR #38.** | 3 | 4 | 12→0 (resolved) | Added an inline `json-body` htmx extension (no new CDN dependency) that serialises form parameters as real JSON and coerces `rating`/`attraction_id` to numbers. | If a future form is added without `hx-ext="json-body"`, it will silently fail the same way — check this first if a new form 400s with no visible reason. | Live curl verification against the running containers, referenced in the PR #38 summary. |
| R-06 | The "remembered filter" behaviour in `refreshAttractions()` (`currentCategory`) did not actually capture the active category filter, since the filter buttons aren't inside a form and don't submit named parameters — `event.detail.requestConfig.parameters.category` was always `undefined`. **Confirmed broken via a real browser click-through, then fixed and re-verified.** | 3 | 2 | 6→0 (resolved) | Confirmed with Playwright against the integrated app (not curl): filtered to "Restaurants," deleted a card, and the list reset to "All" instead of staying filtered — `evidence/01-currentCategory-bug-confirmed-reset-to-all.png`. Root cause traced to `renderAttractions()` reading `event.detail.requestConfig.parameters.category`, which is only ever populated from form/input values, never from a URL baked into a plain button's `hx-get`. | Fixed by setting `currentCategory` directly in each filter button's own `hx-on:click` (e.g. `hx-on:click="currentCategory = 'restaurant'"` on the Restaurants button, `= null` on "All") instead of reading it back from the completed request; `renderAttractions()` no longer touches `event.detail.requestConfig` at all. Re-verified in-browser: filtered to "Restaurants," deleted every remaining restaurant one at a time, and the list correctly stayed on that filter throughout, including the empty-category state after the last one — `evidence/02-currentCategory-bug-fixed-empty-state-stays-filtered.png`. | `student-3/frontend/app.js`, `student-3/frontend/index.html`; `evidence/01-*.png`, `evidence/02-*.png`. Note: the first re-test attempt appeared to still fail because the test browser had cached a stale `app.js` from before the fix (nginx sends no `Cache-Control` header for static files) — resolved by forcing a fresh fetch of the resource, not by changing the fix itself. |
| R-07 | No finalised agentic-loop development record (`docs/agentic-loop-records/`) exists for student-3 specifically — a gap shared by the whole team, but it blocks marking criteria 4 and 5 for my own submission too. | 4 | 4 | 16 | Run `ai-services/agentic-loop` against a real piece of student-3 work (e.g. the PR #38 change) and finalise the JSON record before the report is written. | If the shared `.NET` loop tool has an issue specific to my environment, raise it with Luc immediately rather than skipping the evidence — it's required, not optional. | `docs/agentic-loop-records/<record>.json` (finalised). |
| R-08 | Release 0 is dated 30 August 2026 in the project specification; the latest commit in the repo is dated 3 September 2026. | 2 | 5 | 10 | Confirm the actual submission/showcase date with the tutor rather than assuming either the spec or the commit history is wrong. | If no extension exists, prioritise closing R-01, R-04 (merge), R-07, and the report/video over any further feature polish. | Tutor confirmation (email/Canvas announcement) — not yet obtained. |
| R-09 | Review CRUD is intentionally partial (create + list only, no update/delete) — could be misread as an oversight rather than a scoped decision if not documented. | 2 | 2 | 4 | Documented explicitly in `requirements.md` §1.2 and `known-issues.md` as a deliberate Release 0 scope decision (attractions are the primary CRUD resource per the registration form's feature description). | If a marker/tutor flags it, point to the registration form's `attractions`/`reviews` table description and this risk entry. | `requirements.md` §1.2; `known-issues.md`. |
