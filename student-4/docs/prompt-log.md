# Student 4 AI Prompt Log

| Date | Owner | Activity | Context supplied | AI contribution | Validation status |
|---|---|---|---|---|---|
| 2026-09-03 | Liam Zelmanowski | Release 0 design validation and implementation kickoff | Approved Budget & Expense Tracker design; repository instructions; project specification; Release 0 brief; root README and Compose; shared Vue/nginx/CSS; Student 1 ASP.NET, EF Core, typed client, Ollama, test, health, and Docker patterns; Student 2 static frontend, nginx, Vitest, CI, and architecture patterns | Identified the Student 4 placeholder state, confirmed the approved architecture against repository conventions, documented two non-blocking stack/integration differences, and created the initial feature context | Design documents require Liam review; no runtime, test, Compose, browser, GitHub Actions, or live-model result claimed |
| 2026-09-03 | Liam Zelmanowski | Database implementation | Approved schema/invariants, Student 1 EF patterns, fixed Student 4 architecture | Implemented EF Core SQLite ownership, migration, constraints, HTTP CRUD/filtering, health, transactional idempotent 12/24 seeds, stable errors, and integration tests | `dotnet test student-4/database/tests/Database.Tests.csproj --configuration Release --no-restore`: 8 passed, 0 failed |
| 2026-09-03 | Liam Zelmanowski | Backend and application-AI implementation | Public/internal API requirements, fixed-rate and dashboard rules, Student 1 typed-client/Ollama patterns | Implemented typed database transport, authoritative conversion snapshots, deterministic dashboard, strict Ollama schema/semantic validation, one corrective retry, fallback, public CRUD, and tests | `dotnet test student-4/backend/tests/Backend.Tests.csproj --configuration Release`: 26 passed, 0 failed |
| 2026-09-03 | Liam Zelmanowski | Frontend, shared integration, Compose, and CI | Required user flows, shared visual/routing conventions, Student 2 static frontend/CI patterns | Implemented semantic responsive static UI, safe DOM rendering, full browser CRUD, preview/advice states, local HTMX bundle, shared card/routes, Compose graph, and full CI workflow | Student 4 Vitest: 8 passed; Student 4 package and shared Vue build passed; Docker unavailable, so Compose/image/runtime checks remain pending |
| 2026-09-03 | Liam Zelmanowski | Independent review and corrective pass | Two read-only code-review passes plus a direct database/backend HTTP smoke over Student 4 source and shared integration | Fixed multi-period/case-insensitive seed keys, atomic journey-currency enforcement, omitted-date validation, malformed dependency mapping, null-note transport, stale reload state, mutation error propagation, period-specific row totals, configured-rate seed snapshots, and lazy shared nginx upstreams | Final reviewer verdict PASS; database 12/12, backend 31/31, frontend 10/10; direct smoke passed 12/24 counts, CRUD, conversion, dashboard, and fallback; Docker/human evidence still pending |
| 2026-09-03 | Liam Zelmanowski | Developer command consolidation | Existing Student 4 test, build, startup, Compose smoke, CI, and README commands | Added root npm aliases and reusable PowerShell validation/start/stop/container-smoke scripts; changed CI to call the same validation commands used locally; shortened setup documentation | `npm run validation`, `npm run fe-test`, `npm run be-test`, and `npm run db-test` passed; all PowerShell scripts parse; Docker execution remains pending because Docker is unavailable on PATH |
| 2026-09-03 | Liam Zelmanowski | Pull request packaging | Current staged, modified, and untracked Student 4 tree; service dependency order; validation scripts; repository no-commit/no-push rule | Created three local `LZ/*` branch names and documented an exact stacked PR, commit, file, validation, and merge plan | `npm run validation` passed; four PowerShell scripts parsed with zero errors; branches created; Docker and GitHub Actions execution remain pending |
| 2026-09-04 | Liam Zelmanowski | Student 4 package command cleanup | Student 4 README, root README, CI workflow, PR plan, frontend package scripts, generated Docker/SQLite runtime files | Updated command references to use the Student 4 frontend package and `start`/`stop` aliases; added Student 4-local ignore rules for launch settings and runtime SQLite files | `npm --prefix student-4/frontend run --silent fe-test`: 10 passed; `npm --prefix student-4/frontend run --silent validation`: All passed |
| 2026-09-04 | Liam Zelmanowski | Release 0 startup, testing, Compose, and CI review [OBSERVE]/[ADAPT] | ASD 2026 specification; Release 0 brief; root README; root Compose; `.env.example`; Student 4 startup/test/container scripts, workflow, docs, frontend/backend/database code; Student 1 and Student 2 comparison points | Reviewed Student 4 against Release 0 and Student 1/2 patterns, separated implementation alignment from missing evidence, and recorded required follow-up evidence for full integrated startup, Docker, live Ollama, GitHub Actions, browser checks, and the shared two-model loop | Source validation confirmed frontend 10/10, backend 31/31, database 12/12, and direct shared Vue build pass. `docker compose config --quiet` passed. The previous container smoke built all four targeted images and reached healthy containers, then failed because an already-running shared `ollama` made the fallback-only assertion environment-sensitive. Scoped restart passed; health endpoints returned 200; `/budget/` served; container counts were 12 budgets/24 expenses; live advice returned `source: ai`. |
| 2026-09-04 | Liam Zelmanowski | Student 4 startup script alignment [ADAPT] | Liam's OBSERVE feedback; Release 0 single-Compose requirement; Student 1 and Student 2 script/workflow patterns; Student 4 package scripts, workflow, README, PR plan, requirements, feature plan, and scripts index | Removed Student 4-only container lifecycle scripts and npm aliases, kept source validation aliases, and moved Student 4 CI Docker checks inline using the shared root Compose file like the existing workflow pattern | Edited-file diagnostics passed; no stale lifecycle-command references remain; `npm --prefix student-4/frontend run validation` completed successfully; `docker compose config --quiet` passed. |
| 2026-09-04 | Liam Zelmanowski | Student 4 frontend image install speedup | Student 4 frontend Dockerfile, package manifest, lockfile dependency tree, build copy script, and comparison with static student frontends | Changed the frontend Docker build to run `npm ci --omit=dev --no-audit --no-fund`, so the served image installs only the runtime `htmx.org` dependency instead of Vitest/jsdom/Vite test dependencies | `docker compose build student4-frontend` passed; the install layer added 1 package in 549 ms and the full image build completed in 3.5 s. |
| 2026-09-04 | Liam Zelmanowski | Student 4 CI trigger deduplication | Student 4 GitHub Actions workflow and the observed duplicate `push` plus `pull_request` checks on an open PR | Restricted the Student 4 `push` workflow trigger to `main`, leaving PR branch updates covered by `pull_request` and direct/merged main changes covered by `push` | `git diff --check -- .github/workflows/student-4.yml` passed. |

## Logging Rules

- Add an entry whenever AI materially changes design, code, infrastructure, or tests.
- Record the actual context, bounded change, and observed validation result.
- Do not record an unrun command as passing evidence.
- The shared two-model development-loop record is stored under
  `docs/agentic-loop-records/` and requires Liam's explicit final decision.