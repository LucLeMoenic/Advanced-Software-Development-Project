# Prompt Log

Record meaningful AI-assisted code, infrastructure, test, or design work. Do not log purely conversational help. Do not include secrets or unnecessary source content.

| Date | Task/prompt summary | Context supplied | AI output | Human decision | Validation/evidence |
|---|---|---|---|---|---|
| 2026-08-31 | Replace the Australia-heavy demonstration data with a clean international dataset and meaningful saved ranking explanations. | Current accommodation/search schema and constraints, frontend snapshot contract, the user's request for varied locations, and the requirement for at least 10 records per submitted table. | Replaced existing rows through database HTTP endpoints with 10 trips across Tokyo, Paris, New York, Rome, Barcelona, Singapore, Vancouver, Cape Town, Reykjavik, and Dubai. Added five matching accommodations and five tailored ranking reasons for each trip. | Used synthetic `ai` snapshots because the schema supports only `ai` or `fallback`; documentation explicitly prevents treating these records as live Ollama evidence. No runtime seeder or committed seed script was added. | Database APIs return 10 trips and 50 accommodations. Every trip filter returns exactly five eligible rows, every snapshot contains five results, and all reasons are non-empty and within the 200-character contract. |
| 2026-08-31 | Expand tracked demonstration data so at least 10 trips exist and every trip has multiple eligible accommodation rows. | Existing 14 search records, their destination/guest/budget criteria, the 10-record Sydney catalogue, database HTTP APIs, and the no-runtime-seeder constraint. | Added 10 Melbourne accommodations priced and sized to match all Melbourne trips and five group-capable Sydney accommodations to cover the restrictive Sydney trips. | Kept the existing 14 trips and eight real provider imports. Added only persistent demonstration rows through the database API; no startup seeder or direct SQLite write was introduced. | The tracked database returns 14 trips and 25 active accommodations. Replaying each trip's database filters shows a minimum of five eligible rows per trip; all Melbourne trips return 10 eligible rows and Sydney trips return 5-15. |
| 2026-08-31 | Populate the tracked accommodation catalogue after the live provider correction. | Running database API, the eight validated Sydney records imported from LiteAPI, FR-16's minimum count, and the existing no-runtime-seeder decision. | Created two representative Sydney accommodations through `POST /api/data/accommodations`, bringing the tracked active destination catalogue to 10 records without adding seed code. | Kept the HTTP/database-service boundary and persistent SQLite file. Added only the two records needed to meet the required count and improve capacity/price variety. | `GET /api/data/accommodations?destination=Sydney&active=true` returns 10 records; a search through `http://localhost:5101/api/searches` returns 10 results with `importedProviderData: false`. |
| 2026-08-31 | Diagnose and correct the live LiteAPI sandbox response failure encountered from the traveller search. | Browser `502` response and correlation ID, backend logs, the configured live sandbox response shape, provider documentation, `LiteApiClient`, and focused provider tests. No credential value was copied into repository files or documentation. | Added `includeHotelData: true` so rate responses contain the hotel metadata required for catalogue imports, changed `offerRetailRate` from the incorrect array DTO to the live object shape, skipped isolated rates missing metadata when usable entries remain, and aligned deterministic fixtures with the observed contract. | Kept the existing backend-only credential boundary, 10-result limit, validation, database-API import flow, Ollama ranking, and persistence architecture. | Backend tests pass 62/62. The rebuilt integrated request imported eight usable Sydney records, persisted them through the database API, returned ranked fallback results, and exposed `importedProviderData: true`. This was Copilot-assisted implementation; local Ollama agentic-loop models did not perform the change. |
| 2026-08-31 | Implement Chunk 7 shared integration and CI on the current Chunk 6 branch without modifying the in-progress traveller frontend files. | Chunk 7 plan, FR-18 to FR-20, NFR-10 to NFR-12, current dirty worktree, shared Vue home page, root Compose, Student 1 workflow, setup documentation, and integration risk plan. | Added the shared Accommodation Recommender entry and `/accommodation/` reverse-proxy route, isolated shared/accommodation asset paths, shared health/dependency wiring, complete frontend and .NET CI restore/test/build steps, shared integration image validation, and exact root setup/model/test/health/stop instructions. | Kept the existing Student 1 service boundaries, model setup services, service DNS, and tracked SQLite bind mount. Did not modify the in-progress Chunk 6 Vue files or introduce automatic accommodation seeding. | Shared and Student 1 frontend builds pass; frontend tests pass 7/7; backend tests pass 46/46; database tests pass 19/19; Compose validates; all four integration images build; shared home, `/accommodation/`, accommodation assets, API proxy, and five service health checks pass; 12 search records persist across a database-container restart. This was Copilot-assisted implementation; local Ollama models did not run. |
| 2026-08-31 | Simplify the Chunk 6 frontend around a compact search-first layout. | Human feedback that the interface should read as a clean search/filter bar at the top with results after a short scroll, plus the componentised Chunk 6 implementation and existing accessibility behavior. | Reduced the hero, converted the search form to a compact responsive filter grid, placed results directly after search, and moved saved history below the primary search/results flow. | Kept all labelled controls, field feedback, live status, focus behavior, fallback states, and history CRUD while removing the competing side-by-side layout and decorative hero statistic. | Pending frontend tests and production build for the revised layout. This was Copilot-assisted implementation; local Ollama models did not run. |
| 2026-08-31 | Implement and componentise the Chunk 6 traveller frontend. | Chunk 6 plan, FR-01/FR-03/FR-05/FR-10/FR-12 to FR-14/FR-18/FR-20, NFR-04 to NFR-07, backend API contracts, existing Vue placeholder, shared frontend styles, current risk register, and human request to separate the core interface pieces. | Replaced the placeholder with a typed Vue search and history interface covering client feedback, loading, AI/fallback/empty/dependency states, ranked cards, reopen without reranking, inline rename, confirmed delete, live announcements, focus movement, responsive layouts, and text-only interpolation; separated search input, history CRUD, and results into focused components and added component tests. | Kept `App.vue` as the coordinator, the search form responsible for input/validation, history responsible for its CRUD lifecycle, and results responsible for presentation. Corrected review findings for an initial-history race, stale displayed titles after rename, history-action focus, rename announcements, and long unbroken text. Used CSS variable fallbacks because the shared frontend currently exposes direct values rather than reusable tokens. Added only Vitest, Vue Test Utils, and jsdom for browser-state coverage. | `npm --prefix student-1/frontend test` passes 7/7 tests; `npm --prefix student-1/frontend run build` passes TypeScript checking and the Vite production build; `git diff --check` passes. Manual 320/768/1280px browser execution and integrated screenshots remain pending. This was Copilot-assisted implementation and a Copilot-simulated review; local Ollama models did not run. |
| 2026-08-31 | Critically audit Release 0 requirements, current accommodation docs, placeholder infrastructure, and AI setup; rewrite the requirements and phased plan for full-mark traceability. | ASD project specification, Release 0 brief, feature docs, README, Dockerfiles, root Compose, shared index, and `student-1.yml`. Tutor clarification was added during review: any frontend/backend stack is permitted; the app uses one LLM through its backend; the separate loop uses two distinct local Ollama models for implementation and review. | Replaced vague requirements with IDs/acceptance criteria; created gated phases; separated the one-model app path from the two-model development loop; added risk/evidence controls and versioned development prompts. | Changed the initial AI suggestion that Flask/HTMX was mandatory after the tutor clarification. Kept Vue 3/TypeScript and ASP.NET Core. Removed the application ranking prompt from the development prompt library after human review identified the boundary error; it will live with backend source and tests. Kept seeded-data-first and deterministic fallback recommendations. | Documentation consistency review and git diff for this change; implementation evidence remains pending. |
| 2026-08-31 | Prevent Copilot from committing or pushing repository changes. | User instruction limited to this repository. | Added an explicit repository instruction prohibiting Copilot from running `git commit` or `git push`. | Kept. This preserves the user's manual Git workflow without disabling their credentials. | `AGENTS.md` working rule. |
| 2026-08-31 | Apply the full-marks audit corrections while retaining Vue 3/TypeScript and ASP.NET Core. | Downloaded ASD specification, tutor technology clarification, current planning docs, placeholder `ai-services`, scripts, Compose, and Student 1 workflow. | Added a shared .NET 8 two-model agentic-loop service, local Ollama configuration, role-specific prompts, context protections, pre/post evidence records, focused tests, Compose wiring, model verification script, and lifecycle-plan corrections. | Rejected the initial Python implementation immediately after human correction and replaced it with .NET 8. Kept the current feature stack. Removed automatic model downloads after human review; verification reports missing models without installing them. The loop cannot write source, run arbitrary commands, commit, or push. | `dotnet test ai-services/agentic-loop/tests/AgenticLoop.Tests.csproj`; `dotnet build ai-services/agentic-loop/AgenticLoop.csproj`; Compose configuration and PowerShell syntax checks passed. Docker image execution remains pending because Docker Desktop was not running. |
| 2026-08-31 | Resolve the accommodation branch's merge conflicts with the latest `main` DevOps changes. | Both sides of `.github/workflows/student-1.yml` and `docker-compose.yml`, plus the feature context, requirements, plan, and risk register. | Combined `main`'s shared and Student 2-5 frontend services with the shared Ollama health/volume configuration and .NET agentic-loop service; retained substantive Student 1 tests and added the standard-folder trigger. | Kept both compatible infrastructure changes, removed duplicate Ollama keys, and did not retain the placeholder-only repository structure check. | Conflict-marker scan, Compose configuration validation, and agentic-loop tests. |
| 2026-08-31 | Remove duplicate implementer and reviewer prompt files so the shared runner has one prompt source of truth. | Runtime prompt-loading code, shared prompt files, feature prompt copies, prompt-library index, and evidence references. | Deleted the two drifting feature copies and updated repository instructions and documentation to link to `ai-services/agentic-loop/prompts/`. | Kept the runtime files because they are the prompts actually loaded and hashed in execution records. Retained the feature prompt-library directory only for non-runtime, feature-specific reusable prompts. | Repository reference scan confirms no references to the deleted prompt filenames remain. |
| 2026-08-31 | Critically audit the merged agentic-loop PR for requirement-free bloat, misleading evidence, and missing safety enforcement. | PR #8 delta, staged prompt cleanup, Release 0 requirements/plans, runner implementation, tests, project file, Compose, and CI. | Kept the justified bounded loop, removed recursive test publish content, enforced Plan/Act and structured Observe output, rejected high-confidence secret content, recorded prompt versions, and corrected stale CI evidence/ownership wording. | Rejected cosmetic file splitting and speculative expansion. Kept only changes tracing directly to FR-22, FR-23, NFR-13, NFR-14, reproducible builds, or accurate assessment evidence. | Focused .NET tests, clean publish inspection, Compose validation, reference scan, and staged diff review. |
| 2026-08-31 | Record Phase 0 integration decisions and create the accommodation sprint backlog. | User-confirmed folder, route, labels, services, ports, theme, model tags, shared-file ownership, requirements, feature plan, and evidence checklist. | Added a requirement-linked backlog and recorded confirmed integration contracts across the context, plan, risk register, checklist, and README. | Kept demonstration hardware and durable tutor-approval evidence open rather than inventing values. Deferred the agreed folder rename until immediately before Phase 2 implementation. | Cross-document contract and requirement-ID review. |
| 2026-08-31 | Remove planning bloat and turn the feature plan into implementation-focused chunks. | Existing feature plan, requirements, confirmed contracts, sprint backlog, runner README, and Release 0 evidence requirements. | Replaced the long phase narrative with eight dependency-ordered functionality chunks containing implementation, tests, and objective completion gates; moved local agentic-loop setup to one final section. | Retained only architecture boundaries, failure/security requirements, tests, integration, and assessment evidence that directly support Release 0. Detailed requirement mapping remains in `requirements.md` and `sprint-backlog.md`. | Cross-checked each chunk against the requirements and confirmed setup commands against the agentic-loop README. |
| 2026-08-31 | Close Phase 0 and remove unnecessary hardware gating from the implementation plan. | User confirmation that both remaining Phase 0 items should be treated as confirmed. | Marked Phase 0 complete and removed RAM checks from the active implementation sequence and local agent setup. | Kept only the confirmed integration contracts required to begin implementation. | Cross-document status check. |
| 2026-08-31 | Implement Chunk 1: standard folder and runnable accommodation services. | Chunk 1 plan, FR-19 to FR-20, NFR-09 to NFR-11, confirmed ports/service names, existing Compose/workflow, and the placeholder feature folders. | Renamed the feature to `student-1`; scaffolded Vue 3/TypeScript, ASP.NET Core backend, and ASP.NET Core/EF Core SQLite database API services; added service information and health endpoints, focused HTTP tests, production Dockerfiles, nginx SPA/health configuration, environment-driven service URLs/connection string, Compose health ordering and volume, and complete Student 1 CI steps. | Kept the agreed .NET/Vue/SQLite stack and removed all Flask/sleep/template behaviour. Limited the work to scaffolding and health rather than starting catalogue, search, or AI functionality. After implementation, Mitchell confirmed the three containers work and report healthy. | `npm run build --prefix student-1/frontend`; backend tests 2/2 passed; database tests 2/2 passed; `docker compose config --quiet`; human-reported Docker build/start/health validation. This was Copilot-assisted implementation, not a local Ollama agentic-loop run. |
| 2026-08-31 | Implement Chunk 2 accommodation catalogue on a dedicated branch and align its EF structure with WiseTech Academy conventions. | FR-04, FR-15 to FR-17, NFR-06, NFR-09 to NFR-10, EV-02; representative WiseTech Academy POCO, `DbContext`, `IEntityTypeConfiguration`, migration, DTO, endpoint, and integration-test files. | Created branch `BCP/Chunk_2-Accommodation_Catalogue`; added the `Accommodation` POCO, separate EF configuration, generated migrations, exact integer-cent SQLite price storage, schema constraints/indexes, explicit request/response/error DTOs, validated CRUD and filters, and isolated HTTP/database tests. | Removed the proposed automatic seeder immediately when Mitchell requested an empty catalogue. Retained FR-16 as an explicit outstanding requirement for manual completion. Refactored the crowded `DbContext` to the WiseTech pattern of a clean context plus `IEntityTypeConfiguration`. | 12/12 database tests pass; database Docker image builds; container becomes healthy; empty catalogue returns `[]`; temporary container CRUD created and deleted one record; Compose configuration and diff checks pass. Copilot subagent review was simulated and did not run local Ollama. |
| 2026-08-31 | Replace generic Student 1 code identifiers with concise accommodation-specific names while preserving the mandated repository structure. | Project specification Section 7.1, root repository structure, Student 1 workflow, Dockerfiles, .NET projects/namespaces/tests, frontend package metadata, and user naming decisions. | Kept the required `student-1/` root and `student1-*` Compose identities; renamed projects to `Backend.csproj`, `Database.csproj`, `Backend.Tests.csproj`, and `Database.Tests.csproj`; set assemblies/namespaces to `Accommodation.*`; and renamed the frontend package to `accommodation-recommender-frontend`. | Rejected the attempted `Mitchell-Harris/` root rename after reviewing the explicit `student-x/` specification with Mitchell. Removed repetitive `MitchellHarris.Accommodation.*` project names because the folder already supplies ownership context. | Backend tests 2/2 and database tests 12/12 pass; frontend production build passes; all three Docker images build; Compose configuration and repository reference scan pass. |
| 2026-08-31 | Refactor the database API's EF usage to follow the full WiseTech Academy pattern. | Current catalogue context, mappings, endpoints, tests, migrations, requirements, and representative WiseTech Academy `DatabaseContext`, entity configuration, repository interface/implementation, and DI registration files. | Renamed the feature context to `DatabaseContext`, retained entity mapping in its separate `IEntityTypeConfiguration`, introduced a scoped `IAccommodationRepository`/`AccommodationRepository`, moved EF queries and persistence behind that repository, updated startup/health/migration metadata, and kept direct context access only in startup and database-constraint tests. | Mitchell selected the full pattern rather than EF structure or repository access alone. Kept the repository focused on current catalogue operations and did not copy WiseTech Academy's unrelated generic repository surface or SQL Server-specific configuration. | Database tests pass 12/12; fresh test hosts apply the existing SQLite migrations; API source contains no `DbContext`, EF namespace, or direct `DbSet` access; VS Code reports no diagnostics. `dotnet ef migrations has-pending-model-changes` was not available because the optional `dotnet-ef` tool is not installed. |
| 2026-08-31 | Complete Student 1 Docker Compose runtime wiring. | Root Compose, Student 1 Dockerfiles, nginx configuration, `.env.example`, confirmed service names/ports, application model contract, and local Docker runtime. | Passed `APPLICATION_MODEL` into the backend, added same-origin `/api/` proxying from the frontend container to the backend service, and corrected the frontend health check to use the container's IPv4 loopback address. | Kept the existing three-service architecture, shared Ollama runtime, Compose DNS names, and persistent SQLite volume. Added no service or dependency. | Compose configuration validates; all three images build; database, backend, frontend, and Ollama containers are healthy; frontend health returns `200`; frontend `/api/` requests reach the backend. |
| 2026-08-31 | Make Compose install configured Ollama models only when they are missing. | Root Compose, `.env.example`, application and agentic-loop model contracts, setup documentation, and the existing persistent Ollama volume. | Added separate one-shot setup services for the application model and development-loop models. Each uses `ollama show` first, calls `ollama pull` only for a missing tag, and must complete before its dependent service starts. | Superseded the earlier manual-only model installation decision at Mitchell's explicit request. Kept application and development setup separate so running the traveller application does not unnecessarily download the larger implementer model. | Compose configuration validates; both setup services completed successfully and reported all currently installed tags without downloading them. |
| 2026-08-31 | Make Student 1 startup print the usable localhost endpoints. | Root Compose service names and ports, current Windows setup instructions, and the existing scripts directory. | Added `scripts/start-student1.ps1`, which runs the targeted Student 1 Compose startup, stops on failure, and prints frontend, backend, database, health, and Ollama URLs followed by Compose status. | Kept the root Compose file authoritative rather than duplicating configuration under `student-1/`. Added no dependency or alternate startup architecture. | Script executed successfully against the current Compose stack and displayed healthy service status and expected URLs. |
| 2026-08-31 | Implement Chunk 3 persisted search history on a dedicated branch. | FR-11 to FR-14, FR-17, NFR-07, Chunk 3 plan, current repository-based EF pattern, existing API error contract, and the user decision to commit a one-time populated SQLite file instead of retaining seed code. | Created `BCP/Chunk_3-Search_History`; added the Search entity/configuration/migration, exact cent storage and database constraints, scoped repository, validated create/newest-first list/get/rename/delete endpoints, immutable JSON snapshots, and focused integration tests. Created 10 representative searches once through the HTTP API, checkpointed them into `database/storage/accommodation.db`, removed all runtime seed code, and changed Compose from a named volume to the tracked database directory. | Kept search history independent from catalogue foreign keys so snapshots survive accommodation deletion. Rejected permanent startup seeding after Mitchell clarified that data should be populated once and committed. Left Accommodation population open for the later functional application flow. | Database tests pass 19/19; EF reports no pending model changes; Compose configuration validates; the rebuilt database container becomes healthy and returns all 10 tracked searches through HTTP; snapshot-after-catalogue-delete, invalid input, malformed JSON, constraints, ordering, rename, and repeated delete are covered. This was Copilot-assisted implementation; local Ollama models were not run. |
| 2026-08-31 | Audit all handwritten EF code against the originally agreed WiseTech Academy pattern after Mitchell identified that `SearchConfiguration` still used one direct `IEntityTypeConfiguration.Configure` method. | Current `DatabaseContext`, Accommodation/Search entities and configurations, both repositories, generated migrations for comparison only, and representative Academy `DatabaseContext`, `BaseEntityTypeConfiguration`, concrete configuration, and repository files. | Added a shared `BaseEntityTypeConfiguration<TEntity>` lifecycle, converted both entity configurations to explicit table/key/properties/indexes sections with nested `Schema.TableName`, and changed `DatabaseContext` to an explicit options constructor and initialized `DbSet` properties. Repositories remain the only application query/persistence layer; generated migration code remains untouched. | Accepted Mitchell's correction that the previous implementation had only separated configuration files rather than following the complete agreed configuration pattern. Kept a typed `TableName` property instead of copying the Academy reflection lookup, added a table-constraint hook because SQLite checks are required, and rejected Academy's virtual `DbSet` convention because this project uses neither lazy-loading proxies nor context-property overrides. | Database tests pass 19/19; EF reports no pending model changes; the handwritten scan finds no concrete configuration implementing `IEntityTypeConfiguration` directly; diff checks pass. |
| 2026-08-31 | Implement Chunk 4 deterministic backend search on branch `BCP/Chunk_4_DescribeChangesMade`. | Chunk 4 plan, FR-01 to FR-05 and FR-09 to FR-14, NFR-02/NFR-08/NFR-09, existing database API contracts, tracked SQLite history, and a focused adversarial review. | Added explicit backend request/candidate/result/history/error DTOs, authoritative validation and normalisation, a three-second database HTTP client, semantic dependency-response validation, deterministic ranking, completed-search persistence, history CRUD endpoints, structured correlation logging, and endpoint/client tests. | Kept SQLite ownership and persistence entirely in the database service. Narrowed stored result snapshots to the existing FR-10 display contract so all 10 tracked records remain reopenable; retained no startup seeder and did not rewrite the tracked data. Corrected review findings for malformed-but-deserializable payloads, collection/create `404` handling, non-JSON bodies, and failure-stage logs. | Backend tests pass 29/29; database tests pass 19/19; backend and database images rebuild; all 10 persisted searches reopen through the backend; Compose and diff checks pass. This was Copilot-assisted implementation, not a local Ollama agentic-loop run. |
| 2026-08-31 | Implement Chunk 5 application AI ranking and deterministic fallback. | Chunk 5 plan, FR-06 to FR-10, NFR-01 to NFR-03/NFR-06/NFR-08, existing backend search orchestration, configured Ollama/model values, application prompt contract, and an adversarial diff review. | Added the versioned backend ranking prompt, a 12-second Ollama generate client, allow-listed ranking input fields, strict whole-response validation, AI-to-candidate result mapping, deterministic fallback with a response notice, ranking-mode persistence, structured failure categories, and focused endpoint/client tests. | Kept one configured application model and no new dependency or service. Model text can control only ID/rank/reason; trusted accommodation display fields are restored from validated database candidates. Corrected review gaps by adding HTTP-error, incomplete-response, and unavailable-endpoint fallback tests. | Backend tests pass 46/46 and `git diff --check` passes. Live model execution remains evidence work because this implementation run used deterministic HTTP/test doubles rather than a genuine Ollama ranking request. |
| 2026-08-31 | Connect uncached accommodation searches to LiteAPI sandbox before Ollama ranking. | Current search/backend/database contracts, official LiteAPI v3 rates OpenAPI documentation, provider security boundary, Vue result states, Compose configuration, and external-service risk plan. | Added a backend-only LiteAPI client, environment-driven key/URL, cache-state query, strict provider response validation, AUD total-to-nightly conversion, database-API imports, post-import eligibility query, provider failure envelopes, frontend import notice, and focused client/orchestration tests. | Used LiteAPI `aiSearch` to preserve the existing free-text destination contract. Corrected review findings that `offerRetailRate` is an array and the provider result limit must be enforced. Kept date-independent imported prices as an explicitly documented university-demo cache rather than adding production quote expiry. The disclosed key was never written or used and must be revoked. | Backend tests pass 61/61; frontend tests pass 7/7; frontend production build, Compose configuration, and diff checks pass. The provider was exercised only with deterministic HTTP doubles; a replacement-key sandbox run remains pending. |
