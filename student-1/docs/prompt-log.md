# Prompt Log

Record meaningful AI-assisted code, infrastructure, test, or design work. Do not log purely conversational help. Do not include secrets or unnecessary source content.

| Date | Task/prompt summary | Context supplied | AI output | Human decision | Validation/evidence |
|---|---|---|---|---|---|
| 2026-08-31 | Critically audit Release 0 requirements, current accommodation docs, placeholder infrastructure, and AI setup; rewrite the requirements and phased plan for full-mark traceability. | ASD project specification, Release 0 brief, feature docs, README, Dockerfiles, root Compose, shared index, and `student-1.yml`. Tutor clarification was added during review: any frontend/backend stack is permitted; the app uses one LLM through its backend; the separate loop uses two distinct local Ollama models for implementation and review. | Replaced vague requirements with IDs/acceptance criteria; created gated phases; separated the one-model app path from the two-model development loop; added risk/evidence controls and versioned development prompts. | Changed the initial AI suggestion that Flask/HTMX was mandatory after the tutor clarification. Kept Vue 3/TypeScript and ASP.NET Core. Removed the application ranking prompt from the development prompt library after human review identified the boundary error; it will live with backend source and tests. Kept seeded-data-first and deterministic fallback recommendations. | Documentation consistency review and git diff for this change; implementation evidence remains pending. |
| 2026-08-31 | Prevent Copilot from committing or pushing repository changes. | User instruction limited to this repository. | Added an explicit repository instruction prohibiting Copilot from running `git commit` or `git push`. | Kept. This preserves the user's manual Git workflow without disabling their credentials. | `AGENTS.md` working rule. |
| 2026-08-31 | Apply the full-marks audit corrections while retaining Vue 3/TypeScript and ASP.NET Core. | Downloaded ASD specification, tutor technology clarification, current planning docs, placeholder `ai-services`, scripts, Compose, and Student 1 workflow. | Added a shared .NET 8 two-model agentic-loop service, local Ollama configuration, role-specific prompts, context protections, pre/post evidence records, focused tests, Compose wiring, model verification script, and lifecycle-plan corrections. | Rejected the initial Python implementation immediately after human correction and replaced it with .NET 8. Kept the current feature stack. Removed automatic model downloads after human review; verification reports missing models without installing them. The loop cannot write source, run arbitrary commands, commit, or push. | `dotnet test ai-services/agentic-loop/tests/AgenticLoop.Tests.csproj`; `dotnet build ai-services/agentic-loop/AgenticLoop.csproj`; Compose configuration and PowerShell syntax checks passed. Docker image execution remains pending because Docker Desktop was not running. |
| 2026-08-31 | Resolve the accommodation branch's merge conflicts with the latest `main` DevOps changes. | Both sides of `.github/workflows/student-1.yml` and `docker-compose.yml`, plus the feature context, requirements, plan, and risk register. | Combined `main`'s shared and Student 2-5 frontend services with the shared Ollama health/volume configuration and .NET agentic-loop service; retained substantive Student 1 tests and added the standard-folder trigger. | Kept both compatible infrastructure changes, removed duplicate Ollama keys, and did not retain the placeholder-only repository structure check. | Conflict-marker scan, Compose configuration validation, and agentic-loop tests. |
| 2026-08-31 | Remove duplicate implementer and reviewer prompt files so the shared runner has one prompt source of truth. | Runtime prompt-loading code, shared prompt files, feature prompt copies, prompt-library index, and evidence references. | Deleted the two drifting feature copies and updated repository instructions and documentation to link to `ai-services/agentic-loop/prompts/`. | Kept the runtime files because they are the prompts actually loaded and hashed in execution records. Retained the feature prompt-library directory only for non-runtime, feature-specific reusable prompts. | Repository reference scan confirms no references to the deleted prompt filenames remain. |
| 2026-08-31 | Critically audit the merged agentic-loop PR for requirement-free bloat, misleading evidence, and missing safety enforcement. | PR #8 delta, staged prompt cleanup, Release 0 requirements/plans, runner implementation, tests, project file, Compose, and CI. | Kept the justified bounded loop, removed recursive test publish content, enforced Plan/Act and structured Observe output, rejected high-confidence secret content, recorded prompt versions, and corrected stale CI evidence/ownership wording. | Rejected cosmetic file splitting and speculative expansion. Kept only changes tracing directly to FR-22, FR-23, NFR-13, NFR-14, reproducible builds, or accurate assessment evidence. | Focused .NET tests, clean publish inspection, Compose validation, reference scan, and staged diff review. |
| 2026-08-31 | Record Phase 0 integration decisions and create the accommodation sprint backlog. | User-confirmed folder, route, labels, services, ports, theme, model tags, shared-file ownership, requirements, feature plan, and evidence checklist. | Added a requirement-linked backlog and recorded confirmed integration contracts across the context, plan, risk register, checklist, and README. | Kept demonstration hardware and durable tutor-approval evidence open rather than inventing values. Deferred the agreed folder rename until immediately before Phase 2 implementation. | Cross-document contract and requirement-ID review. |
| 2026-08-31 | Remove planning bloat and turn the feature plan into implementation-focused chunks. | Existing feature plan, requirements, confirmed contracts, sprint backlog, runner README, and Release 0 evidence requirements. | Replaced the long phase narrative with eight dependency-ordered functionality chunks containing implementation, tests, and objective completion gates; moved local agentic-loop setup to one final section. | Retained only architecture boundaries, failure/security requirements, tests, integration, and assessment evidence that directly support Release 0. Detailed requirement mapping remains in `requirements.md` and `sprint-backlog.md`. | Cross-checked each chunk against the requirements and confirmed setup commands against the agentic-loop README. |
| 2026-08-31 | Close Phase 0 and remove unnecessary hardware gating from the implementation plan. | User confirmation that both remaining Phase 0 items should be treated as confirmed. | Marked Phase 0 complete and removed RAM checks from the active implementation sequence and local agent setup. | Kept only the confirmed integration contracts required to begin implementation. | Cross-document status check. |
| 2026-08-31 | Implement Chunk 1: standard folder and runnable accommodation services. | Chunk 1 plan, FR-19 to FR-20, NFR-09 to NFR-11, confirmed ports/service names, existing Compose/workflow, and the placeholder feature folders. | Renamed the feature to `student-1`; scaffolded Vue 3/TypeScript, ASP.NET Core backend, and ASP.NET Core/EF Core SQLite database API services; added service information and health endpoints, focused HTTP tests, production Dockerfiles, nginx SPA/health configuration, environment-driven service URLs/connection string, Compose health ordering and volume, and complete Student 1 CI steps. | Kept the agreed .NET/Vue/SQLite stack and removed all Flask/sleep/template behaviour. Limited the work to scaffolding and health rather than starting catalogue, search, or AI functionality. After implementation, Mitchell confirmed the three containers work and report healthy. | `npm run build --prefix student-1/frontend`; backend tests 2/2 passed; database tests 2/2 passed; `docker compose config --quiet`; human-reported Docker build/start/health validation. This was Copilot-assisted implementation, not a local Ollama agentic-loop run. |
| 2026-08-31 | Implement Chunk 2 accommodation catalogue on a dedicated branch and align its EF structure with WiseTech Academy conventions. | FR-04, FR-15 to FR-17, NFR-06, NFR-09 to NFR-10, EV-02; representative WiseTech Academy POCO, `DbContext`, `IEntityTypeConfiguration`, migration, DTO, endpoint, and integration-test files. | Created branch `BCP/Chunk_2-Accommodation_Catalogue`; added the `Accommodation` POCO, separate EF configuration, generated migrations, exact integer-cent SQLite price storage, schema constraints/indexes, explicit request/response/error DTOs, validated CRUD and filters, and isolated HTTP/database tests. | Removed the proposed automatic seeder immediately when Mitchell requested an empty catalogue. Retained FR-16 as an explicit outstanding requirement for manual completion. Refactored the crowded `DbContext` to the WiseTech pattern of a clean context plus `IEntityTypeConfiguration`. | 12/12 database tests pass; database Docker image builds; container becomes healthy; empty catalogue returns `[]`; temporary container CRUD created and deleted one record; Compose configuration and diff checks pass. Copilot subagent review was simulated and did not run local Ollama. |
| 2026-08-31 | Replace generic Student 1 code identifiers with concise accommodation-specific names while preserving the mandated repository structure. | Project specification Section 7.1, root repository structure, Student 1 workflow, Dockerfiles, .NET projects/namespaces/tests, frontend package metadata, and user naming decisions. | Kept the required `student-1/` root and `student1-*` Compose identities; renamed projects to `Backend.csproj`, `Database.csproj`, `Backend.Tests.csproj`, and `Database.Tests.csproj`; set assemblies/namespaces to `Accommodation.*`; and renamed the frontend package to `accommodation-recommender-frontend`. | Rejected the attempted `Mitchell-Harris/` root rename after reviewing the explicit `student-x/` specification with Mitchell. Removed repetitive `MitchellHarris.Accommodation.*` project names because the folder already supplies ownership context. | Backend tests 2/2 and database tests 12/12 pass; frontend production build passes; all three Docker images build; Compose configuration and repository reference scan pass. |
