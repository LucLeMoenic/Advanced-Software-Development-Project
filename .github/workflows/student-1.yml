name: Student 1 CI

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build Student 1 frontend
        run: |
          echo "Build steps for student-1 frontend go here"
