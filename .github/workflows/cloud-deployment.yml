name: Cloud Deployment

on:
  workflow_run:
    workflows: ["Student 1 CI", "Student 2 CI", "Student 3 CI", "Student 4 CI", "Student 5 CI"]
    types:
      - completed

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    steps:
      - name: Deploy placeholder
        run: |
          echo "Cloud deployment steps go here"
