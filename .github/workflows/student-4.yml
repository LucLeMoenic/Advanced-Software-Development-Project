name: Student 4 CI

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build Student 4 services
        run: |
          echo "Build steps for student-4 services go here"
