# Travel Logistics & Advisory Service - Sprint Backlog

Student 5 (Alex Chen), Release 0. Owner is Alex Chen throughout. Requirement
IDs refer to `requirements.md`. An item is Done only when its requirements hold
in the integrated Compose stack, not just in isolation.

| ID | Backlog item | Requirements | Definition of done | Status |
|---|---|---|---|---|
| TL-01 | Database microservice - schema and seed | TL-FR-10, TL-NFR-01 | Three tables with cascade deletes; 12 destinations, 14 weather notes, 14 transit options; seed runs only when `destinations` is empty. | **Done** - `167b2f6` (PR #25) |
| TL-02 | Database microservice - JSON CRUD API | TL-FR-10 | List/create/get/update/delete on all three resources; `?destination_id=` filter on both child resources; partial-merge `PUT`; `{"error": ...}` bodies with correct status codes. | **Done** - `167b2f6` (PR #25) |
| TL-03 | Database microservice - tests and container | TL-NFR-05 | 26 pytest tests, each against its own `tmp_path` database; image builds; healthcheck passes on container port 8080 / host 5305. | **Done** - `167b2f6` (PR #25). Evidence: `pytest-database.txt` (`26 passed in 0.84s`), `compose-build.txt`, `compose-ps.txt` (`student5-database ... Up About an hour (healthy)`). |
| TL-04 | Agentic-loop tooling fix (shared service) | supports TL-NFR-05 | The loop accepts `.py` and `.sql` context files and has token limits large enough for this feature's source files. | **Done** - `4543fae` (PR #26), `[OBSERVE]` regex follow-up `319f13b` |
| TL-05 | Backend - HTTP database client | TL-NFR-01, TL-NFR-02 | `db_client.py` is the only route to data; returns `DatabaseResponse(status, payload)`, raises `DatabaseUnavailable` only on transport failure; 5s timeout. No `sqlite3` import anywhere in `backend/`. | **Done** - `5df2930` (PR #34) |
| TL-06 | Backend - JSON CRUD passthrough | TL-FR-10, TL-NFR-03 | `api` blueprint mirrors the database API's shapes; `relay()` is the single chokepoint; transport failure becomes `503 {"error": "database service unavailable"}`. | **Done** - `5df2930` (PR #34) |
| TL-07 | Backend - Ollama client | TL-FR-05, TL-NFR-02, TL-NFR-04 | `ollama_client.py` posts non-streaming to `/api/generate` with the tag from `APPLICATION_MODEL`; 120s timeout; every failure becomes `OllamaUnavailable` -> `503 {"error": "ai service unavailable"}`, distinct from the database 503. No model tag is a literal in calling code. | **Done** - `b6fb921` (PR #36) |
| TL-08 | Backend - grounded advisory endpoint | TL-FR-05 | `POST /api/advisory` loads the destination and its child rows, builds a prompt containing them verbatim, and returns advisory text, model tag and destination row. Unknown id forwards the database 404. Grounding is asserted by test. | **Done** - `b6fb921` (PR #36) |
| TL-09 | Backend - HTMX fragment endpoints | TL-FR-01..04, TL-FR-07..09 | `/ui/` blueprint with 7 templates; reads for options/weather/visa/transit/manage-table; writes for create/update/delete that perform the change and re-render from a fresh read; dependency failures render as 200 fragments. | **Done** - `b6fb921` (PR #36) |
| TL-10 | Backend - test suite | TL-NFR-05 | 68 tests covering passthrough, advisory happy path and grounding, both 503 paths, 404s, and every fragment endpoint, with Ollama and the database mocked so the suite runs offline. | **Done** - `b6fb921` (PR #36). Evidence: `pytest-backend.txt` (`68 passed in 0.54s`). |
| TL-11 | Frontend - HTMX page and nginx edge | TL-FR-01..06, TL-NFR-03 | Single static page, no build step; HTMX pinned + SRI; nginx serves the page, proxies `/ui/` and `/api/` same-origin, allows 180s reads, and answers `/health` without reference to the backend. | **Done** - `9ec2aa1` (PR #37) |
| TL-12 | Compose integration | TL-NFR-04, TL-NFR-05 | All three services in root `docker-compose.yml` on 5105/5205/5305 with healthchecks, dependency ordering behind `ollama-model-setup`, and the database bind mount; feature reachable from the shared home page. | **Done** - `fa948af`, `9adae81` (PR #39). Evidence: `compose-build.txt` (three images `Built`), `compose-ps.txt` (all three `healthy` on 5105/5205/5305). Diagram: "Docker Compose architecture" in `architecture.md`. |
| TL-13 | CI workflow | TL-NFR-05 | `student-5.yml` runs the two suites as separate steps, validates the Compose config, and builds the three images, with no live model required, and is green on GitHub. | **Done** - `fc00e4a`, `9e894c9` (PR #40). Evidence: `ci-run-green.png` (run #92, `logistics-services` succeeded in 24s, every step green), plus local captures in `pytest-database.txt`, `pytest-backend.txt` and `compose-build.txt`. Step description in `testing-evidence.md`; diagram "DevOps pipeline architecture" in `architecture.md`. |
| TL-14 | UX/UI uplift to the shared design system | TL-FR-01..06 | Page adopts Student 2's app shell - header, collapsible trip composer, sidebar+main planner grid, tucked-away admin section - so the five frontends read as one product; print sheet and accessible naming preserved. | **Done** - `6058327`, `546c2a4` (PR #42); naming fix `3286e20` (PR #44) |
| TL-15 | Agentic-loop deliverable - `formatElapsed` | TL-FR-06, criterion 4 | The elapsed-time readout built through a full Plan -> Act -> Observe -> Adapt cycle with distinct implementer and reviewer models, a recorded human decision, and a finalised record. | **Done** - `6dd3fd2` (PR #50); record `20260906T053024Z-ad4b32c67df446e8af9e1fca8cc22044.json`, copied to `docs/evidence/agentic-loop-run-final-record.json`; post-test `formatelapsed-assertions.txt` (`all assertions passed`). Diagram: "Agentic AI workflow" in `architecture.md`. |
| TL-16 | Custom reviewer prompt | criterion 5 | `docs/prompt-library/reviewer-llama32-v2.md` (version `student5-reviewer-llama32-v2`) fixes the shared prompt's template-echo defect and is used by the successful run. | **Done** - shipped with `6dd3fd2` |
| TL-17 | Agentic-loop evidence capture | criteria 4 and 5 | Terminal screenshots of the full run and the finalise command, the finalised record, and the supporting text captures, all under `docs/evidence/`. | **Done** - `cdf8942`. Indexed file by file in `testing-evidence.md`. |
| TL-18 | Release 0 documentation pack | all | Context, requirements, plan, risks, backlog, data design, architecture (with the three required diagrams), prompt log, review record, prompt engineering, testing evidence (with the `student-5.yml` step description), known issues, contribution log; README linked. | **Done** - this folder |
| TL-18a | Test and stack evidence capture | TL-NFR-05 | pytest output for both suites, the image build, and a `docker compose ps` showing all three services healthy, committed under `docs/evidence/` and indexed in `testing-evidence.md`. | **Done** - `pytest-database.txt`, `pytest-backend.txt`, `compose-build.txt`, `compose-ps.txt` |
| TL-19 | Browser evidence for the report | TL-FR-01..09 | Screenshots of the loaded panels, a generated advisory with the elapsed counter running, and the manage table after a create, with the inline edit and delete controls. | **Done** - `ui-filled-panels.png`, `ui-advisory-output.png`, `ui-advisory-loading.png` (`17s` on the elapsed readout), `ui-manage-table.png` (created row id 13). The `hx-confirm` delete dialog is not screenshot-able; that path is covered by `backend/tests/test_ui_fragments.py`. |
| TL-20 | MCP tools - `get_weather`, `check_visa_requirement` | - | Out of Release 0 scope. | **Not started (Release 1)** |
